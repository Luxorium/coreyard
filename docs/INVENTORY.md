# CoreYard functionality inventory

Generated by `scripts/inventory.py` from the live command tree — do not edit by hand.
Run `python scripts/inventory.py` after changing a command, and `--check` in CI.

Every public command appears here with what it changes and what it needs, because a
command with an undocumented support status cannot be qualified for release (REL-01) and
cannot be inventoried (REL-05).

**Effect** is the *most* this command can do, not what it does by default. Where a write
is gated, the **Unlocked by** column names the flag; without it the command reports and
changes nothing outside `out/`. `--dry-run` is accepted throughout and always means
"decide, report, change nothing".

**Needs** names the capabilities from `coreyard/capabilities.py` that the command cannot
run without. `coreyard doctor` reports which of them this installation has. A blank cell
means the command works on any supported installation.


CoreYard 0.1.0 — 43 commands.

| Command | What it does | Effect | Unlocked by | Needs |
|---|---|---|---|---|
| `coreyard init` | write a working .env and store.json (start here) | local files |  |  |
| `coreyard status` | what the pipeline believes right now (read-only) | read-only |  |  |
| `coreyard counts` | refresh the exact source-inventory counter shown on the storefront | Shopify | `--apply` | shopify |
| `coreyard doctor` | check the installation, and whether the scheduled jobs are still running | read-only |  |  |
| `coreyard alert` | notify the operator when the pipeline has stopped, and when it recovers | local files |  |  |
| `coreyard sync` | publish the yard to the store (the main loop) | Shopify |  | source |
| `coreyard sync delta` | publish only what changed since the stored cursor (cheap catch-up) | Shopify |  | source, delta |
| `coreyard sync inventory` | availability only: new, revived, repriced, restocked, retired | Shopify |  | source |
| `coreyard sync photos` | media only: parts whose photo set moved on the share | Shopify |  | source, photos |
| `coreyard sync catalog` | copy only: parts whose rendered text, tags, weight or metafields moved | Shopify |  | source |
| `coreyard reconcile` | compare the yard with the live store and close the safe differences | Shopify | `--apply` | source, shopify |
| `coreyard repair` | rewrite catalog output an older renderer produced | read-only |  | shopify |
| `coreyard repair titles` | repair titles | Shopify | `--apply` | source, shopify |
| `coreyard repair tags` | repair tags | Shopify | `--apply` | source, shopify |
| `coreyard repair seo` | repair seo | Shopify | `--apply` | source, shopify |
| `coreyard repair descriptions` | repair descriptions | Shopify | `--apply` | source, shopify |
| `coreyard repair weights` | repair weights | Shopify | `--apply` | source, shopify |
| `coreyard repair metafields` | repair metafields | Shopify | `--apply` | source, shopify |
| `coreyard repair all` | repair all | Shopify | `--apply` | source, shopify |
| `coreyard repair fingerprints` | record the current render as the sync baseline (no Shopify writes) — only when the store is already correct | local files | `--apply` | source |
| `coreyard audit` | read-only listing-quality report | read-only |  | shopify |
| `coreyard audit catalog` | listing-quality report | read-only |  | shopify |
| `coreyard orders` | orders: serve / poll / register / retry / replay / status / sync-status | read-only |  |  |
| `coreyard orders serve` | run the receiver | source database | `--write-orders` | orders |
| `coreyard orders register` | subscribe the store to order webhooks | Shopify |  | orders, shopify |
| `coreyard orders list` | show the store's webhook subscriptions | read-only |  | shopify |
| `coreyard orders unregister` | delete subscriptions | Shopify |  | shopify |
| `coreyard orders replay` | re-run a saved order JSON file | source database | `--write-order` |  |
| `coreyard orders retry` | retry failed stages from retained delivery data | source database | `--write-orders` |  |
| `coreyard orders status` | recent deliveries | read-only |  |  |
| `coreyard orders poll` | pull new orders instead of receiving webhooks | source database | `--write-orders` | shopify |
| `coreyard orders sync-status` | mark Shopify orders the source system has progressed | Shopify | `--apply` | shopify |
| `coreyard images` | list or fetch one part's photos from the share | local files | `--delete --apply` | database, photos |
| `coreyard part-types` | every part type the yard can inventory, and where its wording runs out | read-only |  | source |
| `coreyard schema` | introspect the source database and rank likely tables | read-only |  | database |
| `coreyard validate` | check the external config files against their schemas (offline) | read-only |  |  |
| `coreyard bulk` | resumable concurrent bulk publish | Shopify |  | source, shopify |
| `coreyard altfix` | backfill alt text onto photos published before it was generated | Shopify |  | shopify |
| `coreyard oauth` | exchange a client id/secret for an admin token | local files |  |  |
| `coreyard schedule` | install, remove or show the recurring job | read-only |  |  |
| `coreyard schedule install` | install the recurring job | local files |  |  |
| `coreyard schedule uninstall` | remove it | local files |  |  |
| `coreyard schedule status` | show what is scheduled | read-only |  |  |

## Flags, by command

- **`coreyard init`** — `--city` `--demo` `--env` `--force` `--handle-prefix` `--require-images` `--source` `--store` `--vendor` `--warranty` `--write-schema` `--yes`
- **`coreyard status`** — `--deep` `--json`
- **`coreyard counts`** — `--apply`
- **`coreyard doctor`** — `--install-only` `--json` `--no-network` `--quiet`
- **`coreyard alert`** — `--check` `--json` `--test`
- **`coreyard sync delta`** — `--check` `--deep` `--delta` `--dry-run` `--force-retire` `--image-base-url` `--limit` `--max-retire-fraction` `--no-image-scan` `--out` `--r-number` `--reconcile` `--retire-only` `--sink` `--status`
- **`coreyard sync inventory`** — `--check` `--deep` `--delta` `--dry-run` `--force-retire` `--image-base-url` `--limit` `--max-retire-fraction` `--no-image-scan` `--out` `--r-number` `--reconcile` `--retire-only` `--sink` `--status`
- **`coreyard sync photos`** — `--check` `--deep` `--delta` `--dry-run` `--force-retire` `--image-base-url` `--limit` `--max-retire-fraction` `--no-image-scan` `--out` `--r-number` `--reconcile` `--retire-only` `--sink` `--status`
- **`coreyard sync catalog`** — `--check` `--deep` `--delta` `--dry-run` `--force-retire` `--image-base-url` `--limit` `--max-retire-fraction` `--no-image-scan` `--out` `--r-number` `--reconcile` `--retire-only` `--sink` `--status`
- **`coreyard reconcile`** — `--activate` `--apply` `--dry-run` `--force-retire` `--max-retire-fraction` `--no-retire` `--repair-state`
- **`coreyard repair titles`** — `--apply` `--dry-run` `--limit` `--show` `--status` `--workers`
- **`coreyard repair tags`** — `--apply` `--dry-run` `--limit` `--show` `--status` `--workers`
- **`coreyard repair seo`** — `--apply` `--dry-run` `--limit` `--show` `--status` `--workers`
- **`coreyard repair descriptions`** — `--apply` `--dry-run` `--limit` `--show` `--status` `--workers`
- **`coreyard repair weights`** — `--apply` `--dry-run` `--limit` `--show` `--status` `--workers`
- **`coreyard repair metafields`** — `--apply` `--dry-run` `--limit` `--show` `--status` `--workers`
- **`coreyard repair all`** — `--apply` `--dry-run` `--limit` `--show` `--status` `--workers`
- **`coreyard repair fingerprints`** — `--apply` `--dry-run` `--no-image-scan`
- **`coreyard audit catalog`** — `--all-products` `--json` `--show`
- **`coreyard orders serve`** — `--error-retention-days` `--host` `--no-retire` `--no-write-orders` `--path` `--port` `--write-orders`
- **`coreyard orders register`** — `--topics` `--url`
- **`coreyard orders list`** — _(no options)_
- **`coreyard orders unregister`** — `--url`
- **`coreyard orders replay`** — `--print-cmd` `--retire` `--write-order`
- **`coreyard orders retry`** — `--all` `--id` `--no-retire` `--no-write-orders` `--write-orders`
- **`coreyard orders status`** — `--limit`
- **`coreyard orders poll`** — `--check` `--include-unpaid` `--limit` `--no-retire` `--no-write-orders` `--since` `--write-orders`
- **`coreyard orders sync-status`** — `--apply`
- **`coreyard images`** — `--apply` `--delete` `--dest` `--fetch`
- **`coreyard part-types`** — `--catalogue` `--gaps` `--out` `--parts`
- **`coreyard schema`** — _(no options)_
- **`coreyard validate`** — `--orders` `--overrides` `--profile` `--shipping` `--store` `--weights`
- **`coreyard bulk`** — `--all-parts` `--images-only` `--limit` `--log` `--max-attempts` `--no-resume` `--status` `--workers`
- **`coreyard altfix`** — `--dry-run` `--limit` `--log` `--max-pages` `--no-resume` `--overwrite`
- **`coreyard oauth`** — _(no options)_
- **`coreyard schedule install`** — `--backend` `--dry-run` `--every` `--task`
- **`coreyard schedule uninstall`** — _(no options)_
- **`coreyard schedule status`** — _(no options)_

## Legacy entry points

Every command below is also reachable as `python -m <module>`, and those paths are still
tested. This installation's crontab named them for a long time, and a scheduled job that
changes behaviour silently costs a day before anyone notices. The one deliberate
difference: `coreyard sync` defaults to `--sink api`, while the legacy `coreyard.run_sync`
entry point keeps its original `csv` default.

