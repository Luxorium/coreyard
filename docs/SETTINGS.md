# Settings

Generated by `scripts/settings.py` from the code that reads each key — do not edit by hand.
Every setting CoreYard looks at is here, whether or not an installation sets it.

**Precedence, once, for all of them:** a real environment variable wins; otherwise the value
in `.env` under the data root; otherwise the default in the table. `.env` never overrides an
environment variable that is already set, so a one-off `SOURCE_MAX_AGE_HOURS=0 coreyard sync`
does what it looks like it does. Comments must be on their own line — the parser does not
strip a trailing `#`.

A **switch** is true for `1`, `true`, `yes` or `on`, and false for `0`, `false`, `no`, `off`
or empty. Everything else is text; where a number is expected, the text is parsed as one.

A setting marked **required** has no usable default: the command that needs it refuses to
start and names the key. Which commands need which is what `coreyard doctor` reports, by
capability rather than by key.

Renamed keys keep working — `config._LEGACY_KEYS` maps the old spelling to the new one and
the first run that sees an old name says so on stderr.


## Source database

| Setting | Kind | Default | Read by |
|---|---|---|---|
| `YMS_DB_HOST` | text | empty | `coreyard/config.py` — was `PL_DB_HOST`, which still works |
| `YMS_DB_NAME` | text | empty | `coreyard/config.py`, `coreyard/yms/db.py` — was `PL_DB_NAME`, which still works |
| `YMS_DB_PASSWORD` | text | empty | `coreyard/config.py` — was `PL_DB_PASSWORD`, which still works |
| `YMS_DB_PORT` | text | `1433` | `coreyard/config.py` — was `PL_DB_PORT`, which still works |
| `YMS_DB_USER` | text | empty | `coreyard/config.py` — was `PL_DB_USER`, which still works |
| `YMS_WRITE_ORDERS` | switch | `False` | `coreyard/capabilities.py`, `coreyard/orders/pipeline.py` |

## SMB transport — the database pipe and the photo share

| Setting | Kind | Default | Read by |
|---|---|---|---|
| `SMB_HOST` | text | empty | `coreyard/config.py`, `coreyard/source/database.py` |
| `SMB_IMAGES_SHARE` | text | unset | `coreyard/capabilities.py`, `coreyard/config.py` |
| `SMB_INVENTORY_SUBDIR` | text | `Inventory` | `coreyard/config.py` |
| `SMB_PASSWORD` | text | empty | `coreyard/config.py` |
| `SMB_SERVER_NAME` | text | empty | `coreyard/config.py` |
| `SMB_USER` | text | empty | `coreyard/config.py` |
| `SMB_VEHICLE_SUBDIR` | text | `Vehicle` | `coreyard/config.py` |

## Shopify

| Setting | Kind | Default | Read by |
|---|---|---|---|
| `SHOPIFY_ADMIN_TOKEN` | text | unset | `coreyard/capabilities.py`, `coreyard/sink/shopify_api.py` |
| `SHOPIFY_API_VERSION` | text | unset | `coreyard/sink/shopify_api.py` |
| `SHOPIFY_CLIENT_ID` | text | empty | `coreyard/sink/shopify_oauth.py` |
| `SHOPIFY_CLIENT_SECRET` | text | unset | `coreyard/capabilities.py`, `coreyard/sink/shopify_oauth.py`, `coreyard/webhook.py` |
| `SHOPIFY_HANDLE_PREFIX` | text | `coreyard` | `coreyard/config.py` |
| `SHOPIFY_OAUTH_REDIRECT` | text | unset | `coreyard/sink/shopify_oauth.py` |
| `SHOPIFY_OAUTH_TIMEOUT` | text | `600` | `coreyard/sink/shopify_oauth.py` |
| `SHOPIFY_SCOPES` | text | unset | `coreyard/sink/shopify_oauth.py` |
| `SHOPIFY_STORE` | text | unset | `coreyard/capabilities.py`, `coreyard/sink/shopify_api.py`, `coreyard/sink/shopify_oauth.py` |
| `SHOPIFY_VENDOR` | text | — (required) | `coreyard/config.py` |
| `SHOPIFY_WEBHOOK_SECRET` | text | unset | `coreyard/capabilities.py`, `coreyard/webhook.py` |

## Storefront policy

| Setting | Kind | Default | Read by |
|---|---|---|---|
| `STORE_CATALOG_OVERRIDES_FILE` | text | empty | `coreyard/config.py`, `coreyard/validate.py` |
| `STORE_CITY` | text | empty | `coreyard/config.py` |
| `STORE_DONOR_PHOTO_LIMIT` | text | empty | `coreyard/yms/images.py` |
| `STORE_FILE` | text | empty | `coreyard/store.py`, `coreyard/validate.py` |
| `STORE_ORDER_POLICY_FILE` | text | empty | `coreyard/validate.py` |
| `STORE_PRESERVED_TAG_PREFIXES` | text | empty | `coreyard/config.py` |
| `STORE_PROFILE_FILE` | text | empty | `coreyard/validate.py` |
| `STORE_PUBLICATIONS` | text | unset | `coreyard/capabilities.py`, `coreyard/config.py` |
| `STORE_REQUIRE_IMAGES` | switch | unset | `coreyard/config.py` |
| `STORE_SHIPPING_POLICY_FILE` | text | empty | `coreyard/validate.py` |
| `STORE_STOREFRONT_URL` | text | empty | `coreyard/backlinks.py` |
| `STORE_WARRANTY` | text | empty | `coreyard/config.py` |
| `STORE_WEIGHT_RULES_FILE` | text | empty | `coreyard/validate.py` |

## Source selection and freshness

| Setting | Kind | Default | Read by |
|---|---|---|---|
| `SOURCE_MAX_AGE_HOURS` | text | unset | `coreyard/source/__init__.py` |

## CoreYard behaviour

| Setting | Kind | Default | Read by |
|---|---|---|---|
| `COREYARD_ALERT_CMD` | text | unset | `scripts/healthcheck.py` |
| `COREYARD_ALERT_COMMAND` | text | empty | `coreyard/alerts.py`, `coreyard/capabilities.py` |
| `COREYARD_ALERT_DELTA_STALE` | duration (e.g. `45m`, `2h`) | unset | `coreyard/alerts.py` |
| `COREYARD_ALERT_ORDERS_STUCK` | duration (e.g. `45m`, `2h`) | unset | `coreyard/alerts.py` |
| `COREYARD_ALERT_REPEAT` | duration (e.g. `45m`, `2h`) | unset | `coreyard/alerts.py` |
| `COREYARD_ENRICH` | text | empty | `coreyard/yms/enrich.py` |
| `COREYARD_FITMENT_CACHE` | text | `1` | `coreyard/yms/fitment_cache.py` |
| `COREYARD_FITMENT_CACHE_DAYS` | text | empty | `coreyard/yms/fitment_cache.py` |
| `COREYARD_FITMENT_CACHE_FILE` | text | empty | `coreyard/yms/fitment_cache.py` |
| `COREYARD_HOME` | text | unset | `coreyard/config.py` — environment only, never read from `.env` |
| `COREYARD_ORDER_BOOKING_ATTEMPTS` | text | unset | `coreyard/orders/pipeline.py` |
| `COREYARD_SCHEMA` | text | unset | `coreyard/capabilities.py`, `coreyard/yms/schema.py` |
| `COREYARD_SMB_CONNECT_ATTEMPTS` | whole number | unset | `coreyard/yms/smb_tds.py` |
| `COREYARD_SMB_CONNECT_BACKOFF` | number | unset | `coreyard/yms/smb_tds.py` |
| `COREYARD_SMB_DEBUG` | text | empty | `coreyard/yms/smb_tds.py` |
| `COREYARD_SOURCE` | text | `database` | `coreyard/capabilities.py`, `coreyard/config.py`, `coreyard/source/__init__.py`, `coreyard/yms/inventory.py` |
| `COREYARD_SOURCE_IMAGES` | text | unset | `coreyard/capabilities.py`, `coreyard/source/tabular.py` |
| `COREYARD_WEBHOOK_ERROR_RETENTION_DAYS` | whole number of days | unset | `coreyard/webhook.py` |

## Other

| Setting | Kind | Default | Read by |
|---|---|---|---|
| `XDG_CONFIG_HOME` | text | unset | `coreyard/schedule.py` |
| `XDG_DATA_HOME` | text | unset | `coreyard/config.py` — environment only, never read from `.env` |

58 settings.
